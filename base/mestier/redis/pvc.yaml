apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mestier-redis
  labels:
    app.kubernetes.io/name: mestier-redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
