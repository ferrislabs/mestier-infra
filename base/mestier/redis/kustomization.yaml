apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Env-agnostic base for a simple, single-node Redis (no HA, official image).
# No auth: reachable only in-cluster. Environments overlay this and set the
# target namespace.
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
