apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production overlay for the Ferriskey database.
namespace: ferriskey

resources:
  - ../../../../base/ferriskey/db

# Per-env patches (storage size, instances, backups...) go here when needed, e.g.:
# patches:
#   - target: { kind: Cluster, name: ferriskey-db }
#     patch: |
#       - op: replace
#         path: /spec/storage/size
#         value: 20Gi
